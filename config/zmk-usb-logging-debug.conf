# USB logging diagnostic configuration for the right/central half.
CONFIG_ZMK_USB_LOGGING=y

# Buffer early boot messages long enough to attach to the CDC ACM console.
CONFIG_LOG_PROCESS_THREAD_STARTUP_DELAY_MS=8000
